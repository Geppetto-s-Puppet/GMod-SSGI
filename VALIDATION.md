# SSGI 検証マニュアル

「動いている」と「SSGIとして正しい」は別物。後者は微細な変化の集合なので、**そのまま眺めても判定できない**。
判定できる形に変換してから見る、というのがこの文書の趣旨。

方法は3つ。**(A) 差分を増幅する**、**(B) 同一画面で並べる**、**(C) 収束解と比べる**。

---

## 0. まず道具

| コマンド | 用途 |
|---|---|
| `ssgi_preset` | 引数なしでプリセット一覧 |
| `ssgi_preset <名前>` | 検証用の極端な設定を適用（毎回デフォルトから始まるので混ざらない） |
| `ssgi_ab` | A/B分割ビューのトグル |
| `ssgi_diagnose` | パイプラインの状態を全部コンソールに出す |

デバッグビュー（`pp_ssgi_debug`）：

| 値 | 表示 |
|---|---|
| 0 | 通常合成 |
| 1 | 間接光のみ |
| 2 | 遮蔽のみ |
| 3 | 間接光＋遮蔽（ライティング抜き） |
| 4 | ワールド法線 |
| 5 | 線形深度 |
| 6 | **A/B分割**（マゼンタ線の左＝素、右＝効果あり） |
| 7 | **差分×増幅**（変わった画素だけが光る） |

---

## 1. 一番大事な一枚：差分ビュー

```
ssgi_preset diff
```

`pp_ssgi_debug 7` は `|合成後 − 合成前| × pp_ssgi_diffgain` を出す。**効果が触った画素だけが光る。**

**正しい絵：**
- 壁と床の**接合線**、階段の**蹴込み**、柱の**根元**、プロップの**接地部**が明るい線として浮く
- 平坦な床のど真ん中、開けた空、遠景は**真っ黒**
- 光っている領域が**面ではなく縁**に沿っている

**壊れている絵：**
| 見え方 | 意味 | 触る場所 |
|---|---|---|
| 画面全体が均一に光る | 遮蔽が距離無関係にかかってる | `pp_ssgi_radius` を下げる |
| 空にも差分が出る | 空判定が効いていない | 深度バッファ（`ssgi_diagnose`） |
| 差分が**格子状／ブロック状** | アップサンプルが破綻 | `pp_ssgi_upsample_z` / `_n` |
| 動かすと差分が**ちらつく** | テンポラルが収束していない | `pp_ssgi_temporal_alpha` |
| そもそも真っ黒 | 効果が出ていない | `ssgi_diagnose` |

`pp_ssgi_diffgain` は 12 が既定。見えなければ 30〜60 まで上げていい。

---

## 2. A/B分割

```
ssgi_preset ab
```

マゼンタの縦線を境に、左が素の画面、右が効果あり。**スクリーンショット1枚でbefore/afterが揃う**ので、送るならこれが一番情報量が多い。

`pp_ssgi_split` で境界位置を動かせる。柱や階段が境界をまたぐ位置に立つと差が読みやすい。

**見るべき点：** 境界線をまたぐ床や壁で、**明るさの段差が縁の周辺だけに出ているか**。画面全体の明るさが左右で違うなら、それはGI/AOではなく単なるトーンの狂い（→ 5節）。

---

## 3. 各要素の単体検証

### 3-1. 遮蔽（AO）

```
ssgi_preset ao
```

遮蔽だけを2.5倍にして生で出す（白＝遮蔽なし、黒＝遮蔽）。

- ✅ 隅・角・接地部が黒い。開けた平床は**ほぼ純白**
- ✅ カメラを前後させても、遮蔽の**大きさが世界サイズとして一定**（近づいても遮蔽の輪が広がらない）
- ❌ 平床が灰色 → 自己遮蔽。`pp_ssgi_thickness` か法線精度（→ 5節）
- ❌ 手前のプロップの周りに**背景側へはみ出す暗い縁** → `pp_ssgi_thickness` が大きすぎ
- ❌ 遮蔽がカメラ距離で伸縮 → 半径のスクリーン投影がおかしい（報告して）

gm_construct なら**階段**が最良のテスト対象。段の内側が暗く、踏面の手前側が明るいはず。

### 3-2. 色のにじみ（GI）

```
ssgi_preset gi
```

遮蔽を切って間接光を4倍にする。**赤レンガの壁や緑の草地に1〜2m まで近づく。**

- ✅ 床／隣の白い面が**壁の色に染まる**
- ✅ 離れると染まりが**滑らかに消える**
- ✅ 壁と床の**角に近いほど濃い**
- ❌ 色が付かない → GIが出ていない（`pp_ssgi_debug 1` で確認）
- ❌ 画面全体が均一に染まる → 半径が広すぎ／指向性が失われている
- ❌ カメラを振ると染みが**遅れて追ってくる** → テンポラルのゴースト（→ 3-4）

gm_construct のレンガ壁と白コンクリの境目が一番分かりやすい。

### 3-3. ノイズ量（デノイザの仕事）

**同じ場所に立ったまま**、この2つを撮り比べる：

```
ssgi_preset noise
```
→ 1/4解像度・低品質・テンポラルなし・デノイズなし。**生のトレース結果**。ザラザラで当然。

```
ssgi_preset clean
```
→ **トレース設定は完全に同じまま**、テンポラル＋à-trous 3回＋5×5 を有効化。

この2枚の差が、デノイザが担っている仕事の全量。同じ入力なので、差はデノイズだけに由来する。

- ✅ `clean` でザラつきが消え、**遮蔽の輪郭は残っている**
- ❌ `clean` で輪郭までボケる → `pp_ssgi_denoise_z` / `_n` が緩すぎ（下げる）
- ❌ `clean` でもザラつく → `pp_ssgi_denoise` 回数を増やす、または `pp_ssgi_temporal_alpha` を下げる

### 3-4. テンポラルの破綻（ゴースト）

```
ssgi_preset ghost
```

履歴を極端に粘らせる。**プロップの横を左右にストレイフ**する。

- ✅ 動いている間だけ尾を引き、**止まると1秒以内に消える**
- ❌ 止まっても**残像が焼き付いたまま** → ディスオクルージョン判定が効いていない
- ❌ 振り向いた瞬間に画面全体が破綻 → 再投影の座標がずれている（この場合は報告して）

既定値（`alpha 0.08` / `clamp 2.0`）に戻せばゴーストはほぼ消えるはず。`ghost` はあくまで**限界を見るため**の設定。

### 3-5. アップサンプル

```
ssgi_preset halo
```

1/4解像度で深度・法線のガードを外す。**シルエットの周りにブロック状のハロー**が出るのが正解（＝壊した状態が意図通り壊れている）。

そのあと：
```
ssgi_preset default
```
→ ハローが消えれば、バイラテラルアップサンプルが仕事をしている。

---

## 4. 収束解との比較（一番厳密）

デノイザ付きの手法が「正しい」かは、**同じアルゴリズムを十分なサンプル数で回した結果**と一致するかで判定する。

```
ssgi_preset reference
```
→ フル解像度・ultra（6スライス×16ステップ）・空間デノイズなし・履歴 alpha 0.02。

**その場から動かず3秒待つ。** 履歴が積み上がってノイズが消える。これが実質的なグラウンドトゥルース。スクリーンショットを撮る。

そのまま動かずに：
```
ssgi_preset default
```
→ 同じ構図でもう1枚。

**2枚が構造的に一致していれば正しい。** 高速設定は「ぼやけている」ことはあっても、**暗い場所と明るい場所の位置が違ってはいけない**。位置がずれるなら、それはデノイザやアップサンプルのバグ。

---

## 5. 今の映像で見えている問題

送ってもらった動画から、既に2つ確定している。

### 5-1. 半径のクランプで falloff がズレていた（修正済み）

ワールド半径をピクセルに投影したあと上限でクランプしていたのに、距離減衰は**クランプ前の半径**を使い続けていた。壁に近づくほど二つがズレるので、遮蔽の大きさが変わり境界が急に断絶する。「近づくにつれハッキリ断絶してサイズも変わる」の正体。

クランプ後のピクセル半径からワールド半径を計算し直すよう修正。さらに下限 `pp_ssgi_minradius`（既定4px）を追加したので、遠景で効果がゼロに落ちなくなる。

### 5-2. スライスが奇数だった（修正済み）

medium は 3 スライスを 2π に配っていたので方位が3方向しかなく、**正反対のペアが存在しなかった**。ホライゾン探索は反対向きのペアで一次誤差が打ち消し合うので、これが無いとカメラを回すたび結果が振れる。「同じ階段でも角度で全然違う」の主因。

スライス数を必ず偶数にした。タップ数は据え置きで方位が倍以上になる。

| 品質 | 旧 slices × steps | 新 slices × steps |
|---|---|---|
| low | 2 × 6 | 4 × 4 |
| medium | 3 × 8 | 4 × 6 |
| high | 4 × 12 | 6 × 8 |
| ultra | 6 × 16 | 8 × 12 |

### 5-3. HDR時の二重リニア化（自動化した）

診断で `HDR enabled: true / mat_hdr_level 2` が確定。HDR時のフレームバッファはトーンマップ前のリニア値なので、さらに二乗すると暗部が潰れて明部が飛ぶ。`pp_ssgi_linear` を3値にして**既定を 2（自動）**にした。

### 5-4. AOが直接光まで暗くしていた

フレームバッファには直接光と間接光が合算済みで分離できない。`pp_ssgi_ao_lightbias`（既定 0.6）で明るい画素ほどAOを弱める。

### 5-5. 空判定が `<=` になっていた（修正済み・重要）

`_rt_WPDepth.a` は `1/(rawDepth*4000)`。**空は rawDepth がちょうど 1** なので `1/4000 = 0.00025`。

ところが `r_shaderlib_depthbuffer 1` だと深度バッファは浮動小数で 1.0 を超えて伸びる（0〜4000ユニットが 0〜1、4000〜8000 が 1〜2…）。つまり **4000ユニットより遠い実ジオメトリは 0.00025 より小さい値**を持つ。

こちらは `a <= 0.00025` で判定していたので、**遠方の実ジオメトリを全部「空」として捨てていた**。「到達可能だけど本当に遠い部分も真っ黒にならない」の正体。

Akabenko が `==` で書いていたのはこの理由。等値判定（ドライバ差を吸収する相対窓つき）に修正した。

### 5-6. デバッグビューで空が素通りしていた（修正済み）

単体検証ビュー（1/2/3/5）で、空だけ元の画像がそのまま出ていた。遮蔽ビューの真ん中に写真の空が写るので「効果が出ていない」ように見える。各ビューでその項の**中立値**（GI=黒、AO=白）を返すよう修正。

さらに `pp_ssgi_debug 8`（`ssgi_preset coverage`）を追加。**マゼンタ＝G-Bufferが無い領域**なので、SSGIが届く範囲の地図がそのまま見える。

### 5-7. まだ残っているもの

| 項目 | 対処 |
|---|---|
| `mat_antialias 1` | MRTとMSAAは競合しうるので推奨は `0`。**Options → Video で設定**すること（cfgに保存されないのでコンソールだと毎回消える） |
| 3Dスカイボックス | 既定でG-Bufferに含まれない。`r_shaderlib_3dskybox 1` で入る |
| 境界の空間的モヤモヤ | 残るなら `pp_ssgi_denoise_z` を下げる |
| 本物の空に効果が出ない | **仕様**。スクリーンスペースに追跡対象が無い |

---

## 6. 報告するときのテンプレ

こちらで判断しやすい形：

1. `ssgi_diagnose` の出力をコピペ
2. `ssgi_preset ab` のスクショ1枚
3. `ssgi_preset diff` のスクショ1枚
4. 気になった箇所は `ssgi_preset reference`（3秒静止）と `ssgi_preset default` の同一構図2枚

「なんかモヤモヤする」でも十分に有効な情報だけど、上の4つが揃うと原因の切り分けが一発で済む。
